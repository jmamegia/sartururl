{
  "name": "urlshortener",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prisma:generate": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^3.2.1",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "sequelize": "^6.8.0",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "prisma": "^3.2.1"
  }
}
